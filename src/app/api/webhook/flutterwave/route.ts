// Backward-compatible alias for dashboards configured with the legacy URL.
// Keep all verification and order-finalization logic in one hardened handler.
export { POST } from '@/app/api/payment/webhook/route'
